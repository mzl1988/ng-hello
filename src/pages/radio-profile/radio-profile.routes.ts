import { RouterModule } from '@angular/router';
import { RadioProfileComponent } from './radio-profile.component';

export const radioProfileRoutes = [
    {
        path: '',
        component: RadioProfileComponent
    }
];
